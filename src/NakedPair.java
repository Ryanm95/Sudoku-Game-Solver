public class NakedPair {
    // TODO: find smallest list size of candidates with for loop(col, row, box).
    // With new for loop check if any are in common
    // If # of groups in common equals smallest length
    // If so then delete the values that are in common from every other candidate list in col, row, or box
}
